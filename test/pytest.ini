[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    slow: 标记为慢速测试
    debug: 需要调试器的测试
    hexrays: 需要 Hex-Rays 的测试
addopts = -v --tb=short
filterwarnings =
    ignore::DeprecationWarning

